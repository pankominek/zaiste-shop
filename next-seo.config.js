const title = "Zaiste shop";
const description = "Sklep od zaiste programuj";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: "Zaiste Shop",
  },
};

export default nextSeoConfig;
